Move all files within the MoveFolder into the Receive Folder.

Default Cron Job Runs every 5 minutes.
To add a new Cron job go into the crontab.bak.

To add more folders add volumes to the DockerFile.
